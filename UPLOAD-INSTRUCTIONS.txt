BLISKO 1.5 - FAMILY CONVERSATION UPGRADE

Upload every file in this folder to the same GitHub Pages folder, replacing the previous build.
Do not upload only app.js: the service worker verifies that all files belong to one matching build.

This update preserves learning progress stored on the device and migrates older backups automatically.
It includes the 1.4.2 fix for the full-screen flash after answering and the refresh effect when tapping word tiles.
It also adds editable family profiles, longer family conversations, intention starters, and answer-dependent reactions.

After deployment, open the hosted app once while online. The service worker will install the verified 1.5 shell and perform one controlled restart if needed.
