BLISKO 1.6 - VOCABULARY EXPANSION

Upload every file and folder from this directory to the same GitHub Pages branch/folder that currently hosts Blisko. Replace the existing files rather than adding a second app folder.

Important:
- Keep the filenames unchanged.
- Upload index.html, app.js, data.js, engine.js, tutor.js, polish.js, storage.js, styles.css, sw.js, manifest.webmanifest, BUILD.txt, and all icons together.
- Existing progress is stored locally and remains compatible with this update.

After deployment, open the hosted app once while online. The service worker will install the verified 1.6 shell and perform one controlled restart if needed.
